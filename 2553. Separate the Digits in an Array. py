class Solution:
    def separateDigits(self, nums: List[int]) -> List[int]:
        res=[]
        for i in nums:
            k=str(i)
            for j in k:
                res.append(int(j))
        return res
